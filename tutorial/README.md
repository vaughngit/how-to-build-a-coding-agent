# Tutorial

Start with:

```text
BUILD-CHAT-BY-HAND.md
```

That is the workbook. It tells you how to create a blank `tutorial/workbench/chat.go`
and build the app by typing each stage yourself.

Use `chat-steps/` as the answer key. Each numbered folder contains a complete runnable
`chat.go` for that stage.
